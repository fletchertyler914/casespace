# mini-case fixture

Used by parity integration tests and manual ingest validation.

- `a.txt` — sample evidence file
- `b.txt` — second sample file

Point a new case source path at this directory, then run **Ingest sources** in the desktop app.
